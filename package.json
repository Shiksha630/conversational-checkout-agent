{
  "name": "conversational-checkout-agent",
  "version": "1.0.0",
  "description": "Conversational Checkout Agent with Claude SDK & Razorpay for AI Agentic Commerce",
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"bgBlue.bold,bgMagenta.bold\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "seed": "npm run seed --prefix server",
    "build": "npm run build --prefix server && npm run build --prefix client"
  },
  "keywords": [
    "razorpay",
    "claude",
    "anthropic",
    "agentic-commerce",
    "conversational-checkout",
    "ai"
  ],
  "author": "Hackathon Submission",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
